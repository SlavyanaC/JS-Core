<div class="user-meme">
    <a href="#/details/:{{_id}}" class="user-meme-title">{{title}}</a>
    <a href="#/details/:{{_id}}"> <img class="userProfileImage" src="{{imageUrl}}"></a>
    {{#if isOwner}}
        <div class="user-memes-buttons">
            <a href="#/editMeme/:{{_id}}" class="user-meme-btn">Edit</a>
            <a href="#/deleteMeme/:{{_id}}" class="user-meme-btn">Delete</a>
        </div>
    {{/if}}
</div>
{{#if isEmpty}}
    <p class="no-memes">No memes in database.</p>
{{/if}}
